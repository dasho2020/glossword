### Will SQL-table names conflict with existent tables after installation? I have only one database ###

Will not, Glossword uses prefix `gw_` for all SQL-tables. You may change this prefix during installation.